# notebooks/

구글 드라이브 원본의 사본을 이곳에 둔다. 파일명은 `notebooks.yml` 의
`file` 값과 같아야 한다.

- `060_apple_price.ipynb`
- `073_mnist_basic.ipynb`
- `085_perceptron_xor.ipynb`
- `108_rnn.ipynb`
- `220_llm.ipynb`

**드라이브 원본을 고치면 이 사본도 반드시 함께 고친다.**
검증 셀을 추가하지 말 것 — 판정은 `checks/verify_results.py` 가
실행 결과를 바깥에서 읽어 한다.
